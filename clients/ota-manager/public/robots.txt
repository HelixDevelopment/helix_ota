# Helix OTA Manager — operator console (Tauri desktop shell + embedded web UI).
#
# This is an authenticated internal operator tool, not a public marketing
# surface — it is deliberately excluded from search-engine indexing. See
# index.html's `<meta name="robots" content="noindex, nofollow">` for the
# page-level twin of this directive (§11.4.190(B) — SEO discipline applied
# honestly: a private admin console is optimized for correctness/usability/
# link-preview quality, not for search ranking).
User-agent: *
Disallow: /
