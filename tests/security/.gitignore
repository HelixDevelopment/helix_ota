# Ephemeral / generated test artifacts — never commit (§11.4.10 / §11.4.30).
# Keys + signing material are generated into a mktemp dir outside the repo and
# rm -rf'd on exit; these defensive ignores cover any stray local generation.
*.pem
*.key
*.crt
*.zip
*.bin
*.sig
ota-server
server.log
*.tmp
