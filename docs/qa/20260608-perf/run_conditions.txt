Helix OTA performance baseline — raw harness evidence
Run date: 2026-06-08
Host: Apple M3 Pro, 11 cores, 18 GB RAM, macOS 15.5 (24F74), Go 1.26.2 darwin/arm64
Host load during run: load averages 16.59 20.75 17.94 (host busy: concurrent :8080 job + co-resident loadtest harness)
Harness: server/tools/loadtest (stdlib black-box), built to /tmp/loadtest; go vet clean; selftest validated
Server: go run ./cmd/ota-server on :8090, plain HTTP, gin ReleaseMode, base path /api/v1
  HELIX_PORT=8090 HELIX_ADMIN_USERNAME=admin@helix.test HELIX_ADMIN_PASSWORD=s3cret HELIX_TOKEN_SECRET=perf
Backends measured:
  - in-memory store (mem_*): default (no HELIX_DATABASE_URL)
  - pgx/PostgreSQL (pgx_*): HELIX_DATABASE_URL=postgres://perf:perf@127.0.0.1:55432/helix
    Postgres: docker.io/library/postgres:16-alpine in podman (applehv VM, 2 CPU / 4 GiB)
Paths: /healthz (no store) and /readyz (repo.GetIdempotent round-trip — representative store-touching GET)
Sweep: -concurrency {1,8,32,128} each -duration 15s
Each run's stdout JSON: <backend>_<path>_c<N>.json ; stderr table: .txt
"Errors (no response)" == concurrency per run: expected context-cancel-at-duration-boundary events (1 in-flight/worker
  at deadline), NOT server errors. Non-2xx == 0 in every run.
RSS = server process (compiled go-build exe, the :8090 listener) sampled via ps -o rss during each level.
