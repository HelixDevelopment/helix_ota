=== local Dockerfile FROM + go.mod version (root cause) ===
# Build stage
FROM golang:1.22-alpine AS builder

# Install build dependencies for CGO and SQLite
RUN apk add --no-cache gcc musl-dev sqlite-dev
--- go.mod go directive ---
go 1.24
toolchain go1.24.9

