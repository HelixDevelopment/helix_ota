# Helix OTA 1.0.0-MVP — namespace.
# Validate (Layer 2): kubectl apply --dry-run=client -f kubernetes/  (or kubeconform)
apiVersion: v1
kind: Namespace
metadata:
  name: helix-ota
  labels:
    app.kubernetes.io/part-of: helix-ota
    app.kubernetes.io/version: 1.0.0-mvp
