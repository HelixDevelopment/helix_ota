# CodeGraph data files — local to each machine, not for committing.
# Ignore everything in .codegraph/ except config (tracked per §11.4.78)
# and this .gitignore itself, so transient files (database, daemon.pid,
# sockets, logs) never show up in git.
*
!.gitignore
!config.json
