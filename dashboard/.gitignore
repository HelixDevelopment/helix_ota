node_modules
dist
*.local
.DS_Store

# Playwright e2e artifacts (regenerated by `npx playwright test`)
/test-results
/playwright-report
/.e2e-ota-server
/.e2e-server.pid
# Ephemeral ed25519 artifact-signing key for the e2e harness — regenerated every
# run by e2e/global-setup.ts, NEVER committed (Constitution §11.4.10).
/.e2e-artifact-key.json

# Vitest / coverage artifacts (regenerated by `npm run test:run`)
/coverage
.vitest
