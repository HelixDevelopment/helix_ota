linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - gosimple
  disable:
    - errcheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
  modules-download-mode: readonly
  tests: true
